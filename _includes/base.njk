<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>{{ title }}</title>
{%- if description %}
<meta name="description" content="{{ description }}">
{%- endif %}
{%- if robots %}
<meta name="robots" content="{{ robots }}">
{%- endif %}
<meta property="og:type" content="website">
<meta property="og:locale" content="de_AT">
<meta property="og:site_name" content="{{ site.name }}">
<meta property="og:title" content="{{ title }}">
{%- if description %}
<meta property="og:description" content="{{ description }}">
{%- endif %}
<meta property="og:image" content="https://www.dr-egarter-scheiflinger.at/images/hero-standbild.webp">
<meta property="og:url" content="https://www.dr-egarter-scheiflinger.at/{{ permalink }}">
<meta name="twitter:card" content="summary_large_image">
{%- if extraHead %}
{{ extraHead | safe }}
{%- endif %}
<link rel="icon" type="image/svg+xml" href="images/favicon.svg">
<link rel="icon" type="image/png" sizes="32x32" href="images/favicon-32.png">
<link rel="apple-touch-icon" href="images/apple-touch-icon.png">
<link rel="stylesheet" href="css/style.css">
</head>
<body>
<a class="skip-link" href="#main">Zum Inhalt springen</a>

{%- if site.ankuendigung.aktiv %}
<div class="announcement-modal-backdrop" id="announcement-modal" data-announcement-text="{{ site.ankuendigung.text }}">
  <div class="announcement-modal" role="dialog" aria-label="Ankündigung">
    <button type="button" class="announcement-modal-close" aria-label="Schließen">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
    </button>
    {% icon "alert-circle", {size: 28, strokeWidth: 1.8} %}
    <p>{{ site.ankuendigung.text }}</p>
  </div>
</div>
{%- endif %}

{% include "partials/header.njk" %}

<main id="main">
{{ content | safe }}
</main>

{% include "partials/footer.njk" %}

<button type="button" class="back-to-top" id="back-to-top" aria-label="Nach oben scrollen">
  <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="19" x2="12" y2="5"/><polyline points="5 12 12 5 19 12"/></svg>
</button>

{% include "partials/cookie-banner.njk" %}

<script src="js/main.js"></script>
</body>
</html>
